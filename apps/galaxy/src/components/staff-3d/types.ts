/**
 * Own copy, not shared with whip-3d/Pattern: the staff's pattern set replaces
 * the whip's "solid" with "mirror" (symmetric from the grip outward) and has
 * no color/length/fold props, the staff is a single fixed-length build.
 */
export type StaffPattern = "rainbow" | "comet" | "pulse" | "galaxy" | "mirror" | "off";
