/**
 * Own copy, not shared with staff-3d/StaffPattern or whip-3d/Pattern: same
 * six-pattern set as the one-piece staff (including "mirror", its
 * signature pattern), kept as its own type per the "own copy" convention
 * used across all three product trees.
 */
export type SplitStaffPattern = "rainbow" | "comet" | "pulse" | "galaxy" | "mirror" | "off";
